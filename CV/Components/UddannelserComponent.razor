@using CV.Models

@if (Profil is null)
{
    <p>Indlæser...</p>
}
else if (Profil.Uddannelser?.Count > 0)
{
    <h2 class="headline">Uddannelser</h2>
    <ul class="education-list">
        @foreach (var u in Profil.Uddannelser)
        {
            <li class="education-item">
                <article class="card">
                    <header class="row">
                        <h3 class="education">
                            <span class="institution">@u.Instituion</span>,
                            <span class="location">@u.Lokation</span> -
                            <span class="institution-name">@u.UddannelsesNavn</span>
                        </h3>
                    </header>
                    <p class="meta">@u.Periode</p>
                </article>
            </li>
        }
    </ul>
}
else
{
    <p>Ingen uddannelser tilgængelige.</p>
}

@code {
    [Parameter] public Profil? Profil { get; set; }
}
